// The Twitter card and the OG card are the same design at the same size, so
// this route re-exports the OG one rather than keeping a second copy in sync.
export { alt, contentType, default, size } from "./opengraph-image";
